# Testing the Label Settings API

This directory contains tests for the `/v1/label-settings` API endpoint.

## Running Tests

To run the tests, you can use the following commands:

```bash
pytest
```

```bash
python -m unittest discover -s tests
```
